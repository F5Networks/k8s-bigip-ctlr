stages:
  - Build
  - Test
  
Build-Image:
  tags:
    - docker-executor
  stage: Build
  image: docker
  script:
    - 
    - docker tag ${SOURCE_IMAGE} ${TARGET_IMAGE}
    - docker push ${TARGET_IMAGE}

Run-Regression:
  tags:
    - docker-executor
  stage: Test
  image: centos
  trigger: lohiya/glider
