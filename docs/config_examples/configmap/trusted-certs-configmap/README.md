## Trusted Certs ConfigMap
This configuration allows to configure the certificates. To configure use CIS deployment parameter "trusted-certs-cfgmap"


## Configuration
```
   args:
     --insecure=false
     --trusted-certs-cfgmap=default/trusted-cert
```

