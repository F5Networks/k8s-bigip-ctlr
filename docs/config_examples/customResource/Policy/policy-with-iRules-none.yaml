apiVersion: cis.f5.com/v1
kind: Policy
metadata:
  labels:
    f5cr: "true"
  name: sample-policy
  namespace: default
spec:
  iRules:
    insecure: none # To remove adding default CIS iRule for insecure VS
    secure: none # To remove adding default CIS iRule for secure VS