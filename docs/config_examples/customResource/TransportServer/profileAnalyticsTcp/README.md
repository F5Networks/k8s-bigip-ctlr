# Transport Server with Profile Analytics TCP

This section demonstrates the option to configure Profile Analytics TCP in transport server.

Option which can use to refer Profile Analytics TCP:

```
#Example
profileAnalyticsTcp: /Common/tcp-analytics
```

## ts-with-profileAnalyticsTcp.yaml

By deploying this yaml file in your cluster, CIS will create a Transport Server containing Profile Analytics TCP on BIG-IP.
