# Virtual Server with Profile Analytics TCP

This section demonstrates the option to configure Profile Analytics TCP in virtual server.

Option which can use to refer Profile Analytics TCP:

```
#Example
profileAnalyticsTcp: /Common/tcp-analytics
```

## vs-with-profileAnalyticsTcp.yaml

By deploying this yaml file in your cluster, CIS will create a Virtual Server containing Profile Analytics TCP on BIG-IP.
