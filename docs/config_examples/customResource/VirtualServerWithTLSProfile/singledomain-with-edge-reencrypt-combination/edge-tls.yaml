apiVersion: cis.f5.com/v1
kind: VirtualServerWithTLSProfile
metadata:
  labels:
    f5cr: "true"
  name: edge-VirtualServerWithTLSProfile
  namespace: default
spec:
  hosts:
    - tea.example.com
  tls:
    clientSSL: tea-secret
    reference: secret
    termination: edge